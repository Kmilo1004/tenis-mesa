// IP local de la máquina donde corre `npm run dev` en /backend. El celular con Expo Go debe
// estar en la misma red WiFi. Si cambia la IP (otra red, otra máquina), actualiza este valor.
export const API_BASE_URL = 'http://192.168.20.31:3000/api/v1';
